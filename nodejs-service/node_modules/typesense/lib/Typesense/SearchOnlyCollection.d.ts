import { DocumentSchema, SearchableDocuments } from "typesense/lib/Typesense/Documents";
import ApiCall from "typesense/lib/Typesense/ApiCall";
export declare class SearchOnlyCollection<T extends DocumentSchema = object> {
    private readonly name;
    private readonly apiCall;
    private readonly configuration;
    private readonly _documents;
    constructor(name: string, apiCall: ApiCall, configuration: any);
    documents(): SearchableDocuments<T>;
}
